properties([pipelineTriggers([pollSCM('* * * * *')])])

node{
checkout([$class: 'GitSCM', branches: [[name: '**/pullrequests/**']], doGenerateSubmoduleConfigurations: false, 
extensions: [], submoduleCfg: [], userRemoteConfigs: [[refspec: '+refs/pull/*/head:refs/remotes/origin/pr/*', 
url: 'https://github.com/Birju-org/sonar.git']]])
}
